name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    container: fenicsproject/dolfinx:latest

    steps:
    - uses: actions/checkout@v2
    - name: build
      run: cd src && cmake . && make